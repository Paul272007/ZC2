#include <header.hpp>

using namespace std;

int header_create(vector<string> files, bool force, string output) {
  cout << "DEBUG: header_create called." << endl;
  return 0;
}
int header_sync(string h, string c, string output) {
  cout << "DEBUG: header_sync called." << endl;
  return 0;
}
int header_init(string h, bool force, string output) {
  cout << "DEBUG: header_init called." << endl;
  return 0;
}
